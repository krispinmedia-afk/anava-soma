{
  "name": "anava-soma",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "generate-types": "wrangler types",
    "deploy": "astro build && rm -f dist/wrangler.json && rm -f .wrangler/deploy/config.json && wrangler pages deploy dist --project-name anava-soma --branch main"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.7.0",
    "astro": "^6.4.6",
    "wrangler": "^4.99.0"
  },
  "overrides": {
    "vite": "^7"
  }
}